on:
  push:
    paths-ignore:
      - '**.md'
      - 'assets/**'
      - '.gitignore'
  pull_request:
    paths-ignore:
      - '**.md'
      - 'assets/**'
      - '.gitignore'
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main